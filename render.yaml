services:
  - type: web
    name: minga-portfolio
    env: python
    buildCommand: pip install -r requirements.txt && python seed.py
    startCommand: gunicorn app:app
    envVars:
      - key: ADMIN_KEY
        generateValue: true
      - key: DATABASE_PATH
        value: portfolio.db
